!IMPORTANT - AI 必须遵循治理规则

# 项目: HyperLens
# 治理 Skill: .agent/skills/project-governance/SKILL.md

## 每次会话开始
1. 读取本文件
2. 检查 INBOX.md → 汇报待办
3. 检查 CHANGELOG.md → 汇报最近改动

## 核心规则
- 小功能 (<100行, <3文件): 直接做
- 大功能/架构变更: 六步法汇报，等用户决策
- 模块化: 禁止直接改主入口，新功能新建模块
- 完成后: 更新 CHANGELOG，问用户测试结果

## 🔒 锁定资产 - 修改需二次确认
 
⚠️ 当前锁定：
**全原生架构 (Pure Native Architecture)**

### 锁定项
- **技术栈**: Rust + Win32 GDI (完全剥离 Web 前端)
  - **Capture/Overlay**: 必须使用 `native_overlay` 模块 (Rust/GDI)
  - **UI/Settings**: 仅允许在 `Settings/Gallery` 使用 React/Webview
- **模块路径**: `src-tauri/src/service/native_overlay/`
- **核心原则**:
  - **Zero Latency**: 输入/渲染必须在 Native 线程完成
  - **Resource Safety**: 必须使用 `AutoGdiObject` (RAII) 管理 GDI 句柄
  - **No Web Interaction**: 截图过程中禁止唤起 WebView

### 修改流程
修改上述锁定项时:
1. **立即停止**
2. 说明: "即将修改锁定资产，变更: [内容]"
3. 等待用户 **"确认"**

## 技术债务
- TD-001: 标注系统尚未实现 (需由 GDI 实现)
- TD-002: 自动更新 (Auto-Updater) 未配置
- TD-003: 崩溃上报 (Crash Reporting) 未集成
