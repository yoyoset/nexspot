!IMPORTANT - AI 必须遵循治理规则

# 项目: HyperLens
# 治理 Skill: .agent/skills/project-governance/SKILL.md

## 每次会话开始
1. 读取本文件
2. 检查 INBOX.md → 汇报待办
3. 检查 CHANGELOG.md → 汇报最近改动

## 核心规则
- 小功能 (<100行, <3文件): 直接做
- 大功能/架构变更: 六步法汇报，等用户决策
- 模块化: 禁止直接改主入口，新功能新建模块
- 完成后: 更新 CHANGELOG，问用户测试结果

## 🔒 锁定资产 - 修改需二次确认
 
⚠️ 当前锁定：
**零延迟捕捉架构** (Native GDI Screenshot + Web Mask)

### 锁定项
- **架构**: 混合渲染 (Hybrid Rendering)
  - 底层: Rust/GDI (负责显示高像素截图)
  - 顶层: Web/CSS (负责遮罩与交互)
- **文件**: `src-tauri/src/service/native_overlay.rs` (核心渲染逻辑)
- **关键技术**: 
  - `UpdateLayeredWindow` (直接像素传输)
  - `box-shadow` 遮罩 (避免复杂 Canvas 计算)
  - `PointerEvents` (多显示器输入捕获)
- **坐标**: Physical (Native) vs Logical (Web) 映射必须严格对齐

### 可调整项 (不锁定)
- 前端 UI 样式 (颜色、图标)
- 快捷键逻辑
- 保存路径逻辑
- WebView overlay (overlay_manager.rs) - 需进一步调试坐标
 
### 修改流程
修改上述锁定项时:
1. **立即停止**
2. 说明: "即将修改锁定资产，变更: [内容]"
3. 等待用户 **"确认"**

## 技术债务
- TD-001: 截图性能 (Debug模式13-15秒)
- TD-002: 选区不响应 (WebView未通)
- TD-003: DevTools无法打开
